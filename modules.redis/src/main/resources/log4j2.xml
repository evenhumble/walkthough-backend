<Loggers>
    <Root level="warn"> <!-- 全局配置 -->
        <AppenderRef ref="Console"/>
    </Root>

    <!-- 为某些包/类配置Log级别，这些logger继承了root logger -->
    <Logger name="io.hedwig" level="DEBUG" additivity="false">
        <AppenderRef ref="Console"/>
    </Logger>
</Loggers>
